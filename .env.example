# Copy this file to .env for local testing with `vercel dev` (plain `vite dev`
# does not run the /api serverless function).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
